#include <iostream>
#include <cassert>
#include "input.h"
#include "interpolation.h"

int main(int* argc, char** argv){
	std::string filename;
	filename = argv[1];
	input parms(filename);

	//interpolate for appropriate format
	
	return 0;
}
